# 20260520-FRONTEND — TRIBBO Redesign suite

## Refontes effectuées aujourd'hui

### Mise en page & Design
1. **Drapeaux en cercle** — match-card et prediction-form : remplacement des émojis `🏳️` par `<img>` cerclés `rounded-full` avec `flagUrl` (données équipes Supabase)
2. **Halo radial** — match-card : fond `radial-gradient` bleu derrière chaque carte
3. **Header prediction-form** — supprimé le bandeau bleu `bg-tribbo-primary`, remplacé par une carte blanche centrée (`rounded-2xl shadow-sm border`) avec son propre halo
4. **Statuts renommés** — `scheduled`→"À pronostiquer", `locked`→"Pronos clos", `finished`→"Match joué" (match-status-badge + match-list filters)
5. **Mobile full width** — `app-root { display:flex; width:100% }`, `app-top-app-bar, app-bottom-nav { display:contents }` dans `styles.scss`
6. **Header fixe** — desktop navbar + mobile TopAppBar passés en `fixed top-0`, main padding ajusté (`pt-14 md:pt-16`)
7. **Landing page SAAS** — refonte complète avec sections Hero (2 colonnes desktop), Features (3 cartes), Stats (2 chiffres), CTA final, Footer
8. **Layout landing desktop** — supprimée la contrainte `max-w-lg` qui bridait la page à 512px sur desktop (cause racine : `isAuthScreen()` incluait `/` dans `app.ts`)
9. **Padding desktop augmenté** — sections landing et app en `md:px-16 lg:px-24`

### Corrections app.html
- `isAuthScreen()` ne concerne plus que `/login` et `/signup`
- Nouveau signal `isLandingPage()` pour `/` et `showAppChrome()` pour les pages internes
- Chrome (header, TopAppBar, footer, bottom-nav) caché sur landing et auth
- Wrapper router-outlet : `max-w-lg` pour auth, `max-w-7xl` pour app, aucun pour landing
- Padding `pt-14 md:pt-16 pb-16 md:pb-0` conditionnel à `showAppChrome()`

### Problèmes restants
- Auth non branché : login/signup ne font pas appel à `AuthService`
- `prediction-form` utilise toujours `DEMO_MATCH` (France-Brésil)
- Signup : input "Nom" sans `formControlName`
- Clés Supabase vides → RPC échouent
- `authGuard` désactivé
- Logo Tribbo : en attente d'un vrai visuel (SVG/PNG)
