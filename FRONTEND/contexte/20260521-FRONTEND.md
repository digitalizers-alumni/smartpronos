# 20260521-FRONTEND — TRIBBO R0 finalisation & attaque R1

## État des lieux US-FE (R0)

| Ticket | Statut | Détail |
|---|---|---|
| **US-FE-001** — Login | ✅ UI faite | Auth pas branchée (clés Supabase vides — délibéré) |
| **US-FE-002** — Profile create | ⚠️ 95% | Signup UI complète, mais input "Nom" sans `formControlName` |
| **US-FE-006** — View match list | ✅ OK | Filtres, date groups, MatchCard, MatchStatusBadge |
| **US-FE-008** — Make prediction | ⚠️ 90% | PredictionForm + validation OK, utilise `DEMO_MATCH` en dur |
| **US-FE-020** — Mobile use | ✅ OK | BottomNav, TopAppBar, responsive grid, touch targets |
| **US-FE-021** — Shared Components | ⚠️ 70% | Models OK, authGuard créé (désactivé), manque loading/error-state |
| **US-FE-PP** (1-6) — Setup projet | ✅ OK | Standalone, Tailwind, Angular 21, routing, layout, etc. |

### Trous restants R0
1. Champ "Nom" dans signup sans `formControlName` (ligne 34-38 de `signup-page.html`)
2. `prediction-form-page` utilise `DEMO_MATCH` au lieu de récupérer le match via `MatchService`
3. Pas de composants `loading-state` (skeleton) ni `error-state` — simple texte "Chargement..."
4. Auth désactivé (attente clés Supabase)

## R1 — Solo Player Loop (à faire)

| Ticket | Priorité | Pages/Composants à créer |
|---|---|---|
| **US-FE-007** — View match detail | P0 | `pages/match/detail` |
| **US-FE-009** — Modify prediction pre-lock | P0 | Mode edit dans `PredictionForm` |
| **US-FE-010** — Lock post-deadline | P0 | Désactiver inputs après deadline |
| **US-FE-012** — Earn points | P0 | Stats bar dynamique + `user-rank-card` |
| **US-FE-031** — Page "Mes pronos" | P1 | `pages/predictions/mine` |
| **US-FE-032** — Déconnexion / logout | P0 | Bouton dans profil |
| **US-FE-033** — Vue match terminé | P0 | Résultat + points dans fiche match |

## Board d'exécution (extrait frontend)

Toutes les tâches frontend de l'execution board sont encore en **TODO**. Backend (tables, RPC, vues) en statut **REVIEW**.

